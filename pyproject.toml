[tool.poetry]
name = "isekai_habit_api"
version = "0.1.0"
description = ""
authors = ["Hieu Nguyen <hieu987020@email.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.103.0"
uvicorn = "^0.22.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"